{
  "mcpServers": {
    "codex-web-search": {
      "command": "C:/path/to/codex-web-search-mcp/target/release/codex-web-search-mcp.exe"
    }
  }
}

// ───────────────────────────────────────────────────────────────────────────
// codex-web-search-mcp v2.0.0（Rust 重写版）MCP 接入示例
//
// 与旧 Node 版最大的区别：Rust 版是独立可执行文件，不再需要 Node / npx！
// 只要指向编译出的 exe（或 macOS/Linux 下的二进制）即可，Claude Code / 任意
// 支持 MCP 的客户端直接 spawn，不经过任何 .cmd 解析，最稳。
//
// 1) Windows（推荐 release 构建）：
//      "command": "D:/codex-web-search-mcp/target/release/codex-web-search-mcp.exe"
//
// 2) macOS / Linux：
//      "command": "/path/to/codex-web-search-mcp/target/release/codex-web-search-mcp"
//
// 3) 可选环境变量（不填也能用，默认走 OpenAI Codex 后端）：
//      CODEX_ACCESS_TOKEN / CODEX_ACCOUNT_ID   —— 覆盖 codex login 的凭证
//      CODEX_SEARCH_BACKEND=grok                —— 切到 Grok/xAI 后端（需下面 key）
//      GROK_SEARCH_API_KEY 或 XAI_API_KEY       —— Grok 后端凭据
//      GROK_SEARCH_URL / GROK_SEARCH_MODEL      —— Grok 自定义端点/模型
//      TAVILY_API_KEY                           —— web_map 工具所需
//      CODEX_SEARCH_CONFIG                      —— 指向 config.toml 的绝对路径
//      （默认读取 ~/.config/codex-web-search-mcp/config.toml）
//
// 4) 凭证必做：先 `codex login`（生成 ~/.codex/auth.json），或设置
//      CODEX_ACCESS_TOKEN。没有凭证任何搜索工具都会报错。
//
// 5) 绝对不要用："command": "cmd", "args": ["/c", ...] —— 会破坏 MCP stdio 管道
//      导致超时 / -32000。
