{
  "mcpServers": {
    "codex-web-search": {
      "command": "node",
      "args": ["<这里填 `npm root -g` 的输出>/codex-web-search-mcp/codex-web-search-mcp.js"]
    }
  }
}

// 说明：
// ★ 最可靠写法（任何 Node 都能用）：先 `npm install -g github:dhicoc/codex-web-search-mcp`，
//   再把上面 args 里的路径换成你机器上 `npm root -g` 返回目录下的
//   codex-web-search-mcp/codex-web-search-mcp.js：
//     - Windows 标准 Node：C:/Users/<用户名>/AppData/Roaming/npm/node_modules/...
//     - macOS/Linux：/usr/local/lib/node_modules/...
//   `node` 是 .exe（Windows）/ 可执行文件，Claude Code 直接 spawn，不经过 Windows .cmd 解析，
//   因此在「受管 / 便携版 Node」（如某些 AI 工具内置的 Node）上也能稳定连上。
//
// 标准 Node 首选（免路径）：若你的 Node 是标准安装（裸 npx 能解析 .cmd），可用社区标准写法：
//   "command": "npx",
//   "args": ["-y", "github:dhicoc/codex-web-search-mcp"]
//   注意：在受管 / 便携 Node（如本工作区捆绑的 Node 22.22.2）上会报 `spawn npx ENOENT` → 30s 超时，请用上面的 node + 路径写法。
//
// 绝对不要用：`"command": "cmd", "args": ["/c", ...]` —— 会破坏 MCP stdio 管道导致超时 / -32000。
